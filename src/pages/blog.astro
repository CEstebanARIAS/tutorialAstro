---
import Layout from "../layouts/Layout.astro";
import Footer from "../components/footer.astro";
import Navegation from "../components/navegation.astro";

const pageTitle = "Mi blog de Astro aprendizaje";
const allPosts = await Astro.glob('../pages/post/*.md');
---

  <Layout title={pageTitle}>
    <main>
        <Navegation/>
      <h1>Mi blog de aprendizaje sobre Astro</h1>
      <p>
        Aquí es donde publicaré acerca de mi viaje aprendiendo Astro.
          {allPosts.map((post) => 
				<li>
					<a href={post.url}>{post.frontmatter.title}</a>
					<h6>Fecha Pulicacion: {new Date(post.frontmatter.pubDate).toISOString().split('T')[0]}</h6>
				</li>
			)}
      </p>
	  	<Footer/>
    </main>
  </Layout>


  <style>
	
	main {
		margin: auto;
		padding: 1rem;
		width: 800px;
		max-width: calc(100% - 2rem);
		color: white;
		font-size: 20px;
		line-height: 1.6;
		
	}
	
	h1 {
		font-size: 4rem;
		font-weight: 700;
		line-height: 1;
		text-align: center;
		
	}
	h6 {
		margin-top: 0;
		font-family: sans-serif;
		font-weight: normal;
	}

	@media screen and (max-width: 512px) {
		h1{
			border-top: solid 1px;
			padding-top: 2rem;
		}
	}
	
</style>
